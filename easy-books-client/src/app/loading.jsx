import Loader from '@/components/Shared/Loader';
import React from 'react';

const Loading = () => {
    return (
        <div>
            <Loader></Loader>
        </div>
    );
};

export default Loading;